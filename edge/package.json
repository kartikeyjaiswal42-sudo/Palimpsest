{
  "name": "palimpsest-edge",
  "private": true,
  "type": "module",
  "description": "Palimpsest hosted on Cloudflare Workers: the interface, the observer and the detector in one Worker.",
  "scripts": {
    "build": "python3 -c \"import sys;sys.exit('run the three build scripts with the project venv: see edge/README.md')\"",
    "test": "node test/parity.test.mjs test/parity-cases.json test/parity-cases-live.json && node test/units.test.mjs && node test/no-generative-calls.test.mjs",
    "test:parity": "node test/parity.test.mjs test/parity-cases.json test/parity-cases-live.json",
    "test:units": "node test/units.test.mjs",
    "test:wrapper": "node test/no-generative-calls.test.mjs",
    "test:live": "node test/live.test.mjs",
    "test:repeat": "node test/repeatability.test.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --remote"
  },
  "devDependencies": { "wrangler": "^4" }
}
